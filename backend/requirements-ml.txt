# Dependencies for the trainable model paths. NOT installed by default.
#
#   Docker:  docker compose build --build-arg INSTALL_ML=true backend
#   Native:  pip install -r requirements.txt -r requirements-ml.txt
#
# Needed for:
#   ml/train_pricing.py            pricing model (needs real invoice history)
#   app/services/ml_pricing.py     serving that model
#   nlp/train_ner.py               v2 spaCy NER (needs annotated queries)

# --- ML pricing -----------------------------------------------------------
# HistGradientBoostingRegressor ships with scikit-learn and handles NaN and
# categorical features natively, so no separate gradient-boosting library and
# no pandas are required. joblib and numpy come in with scikit-learn; they are
# pinned here because the inference path imports them directly.
scikit-learn==1.5.2
joblib==1.4.2
numpy==2.0.2

# --- v2 NLP ---------------------------------------------------------------
spacy==3.7.5
