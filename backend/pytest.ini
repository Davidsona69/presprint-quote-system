[pytest]
asyncio_mode = auto
# Pin the fixture loop scope — pytest-asyncio warns that the default changes in
# a future release, and the table-creation fixture is per-test.
asyncio_default_fixture_loop_scope = function
